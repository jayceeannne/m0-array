var flowerTypes = ["Ranunculus", "Snapdragon", "Canterbury bells", "Poppy"];
var bundleNum = [4, 8, 10, 25];
var depositPaid = true; 

console.log(flowerTypes.pop());
console.log(bundleNum.unshift(5,6));

// .pop printed the last key value inside the array
// array.unshift() gives the array a new length 

const animals = ["puppies", "kittens", "ducklings"];
const count = animals.push('joeys');
console.log(animals);
// has a similar reaction to 'push' but it is added to the beginning of an array..
